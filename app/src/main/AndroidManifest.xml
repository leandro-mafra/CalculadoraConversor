<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mafra.calculadoraconversor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityVelo"
            android:label="@string/title_activity_activity_velo"
            android:theme="@style/AppTheme" />
        <activity android:name=".ActivityComprimento" />
        <activity android:name=".ActivityMassa" />
        <activity android:name=".comprimento.ActivityPressao" />
        <activity android:name=".ActivityPressao" />
        <activity android:name=".ActivityPrePsi"></activity>
    </application>

</manifest>
